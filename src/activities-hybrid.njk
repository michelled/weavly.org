---
permalink: /activities-hybrid.json
---
[
    {%- for activity in collections.hybridActivities %}
    {
        "title": "{{ activity.data.title | safe }}",
        "url": "{{ activity.url }}",
        "type": "{{ activity.data.type | safe }}",
        "experience": "{{ activity.data.experience | safe }}",
        "subject": "{{ activity.data.subject | safe }}",
        "description": "{{ activity.data.description | safe }}"
    }{{ "" if loop.last else "," }}
    {%- endfor %}
]
