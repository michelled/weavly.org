---
permalink: /organizations.json
---
[
    {%- for organization in collections.organizations %}
    {
        "title": "{{ organization.data.title | safe }}",
        "category": "{{ organization.data.category | safe }}",
        "description": "{{ organization.data.description | safe }}",
        "link": "{{ organization.data.link }}"
    }{{ "" if loop.last else "," }}
    {%- endfor %}
]
